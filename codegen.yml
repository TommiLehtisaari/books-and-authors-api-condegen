schema:
  - 'schema/**/*.graphql'
generates:
  'src/generated/graphql.ts':
    # config:
    #   mappers:
    #     Contract: ../models#BookModel
    config:
      contextType: ../context#Context
      useIndexSignature: true
    plugins:
      - typescript
      - typescript-resolvers